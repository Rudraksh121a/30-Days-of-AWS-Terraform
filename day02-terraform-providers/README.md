# Day 02 – Understanding Terraform Providers

Terraform providers act as the bridge between your HCL code and the actual cloud API.  
They translate Terraform configuration into real AWS API calls.

In this module, you will:

- Understand official, partner, and community providers
- Explore provider versioning
- Learn to lock provider versions
- Configure the AWS provider
- Run your first `terraform init` and `terraform plan`
